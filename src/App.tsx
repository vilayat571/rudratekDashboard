import { Dashboard } from "./pages/Dashboard";

// App is kept intentionally minimal.
// As the app grows, a router (e.g. React Router) would go here.
function App() {
  return <Dashboard />;
}

export default App;
